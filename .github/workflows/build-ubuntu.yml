name: Build ubuntu

on:
  push:
    branches: [ "main", "actions/build" ]
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Type of build (debug or release)'
        required: true
        default: 'release'
      target:
        description: 'Optional compilation target'
        required: false


jobs:
  build-ubuntu:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install gvmkit-build
        run: |
          wget https://github.com/golemfactory/gvmkit-build-rs/releases/download/v0.3.19/gvmkit-build-x86_64-unknown-linux-gnu.tar.gz
          tar -xvf gvmkit-build-x86_64-unknown-linux-gnu.tar.gz
          mv gvmkit-build /usr/local/bin
          rm gvmkit-build-x86_64-unknown-linux-gnu.tar.gz
          gvmkit-build -h

      - name: Cuda toolkit
        uses: Jimver/cuda-toolkit@v0.2.19
        with:
          cuda: '12.4.1'

      - name: Build project
        run: |
          python build-cross.py

      - name: Free some space by removing CUDA libraries
        run: |
          df
          sudo rm -rf /usr/local/cuda*
          df

      - name: Create docker
        run: |
          cp profanity_cuda runtime/
          cd runtime
          docker build -t nvidia/profanity-cuda:12.4.1 .
          gvmkit-build nvidia/profanity-cuda:12.4.1

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: profanity_cuda
          path: profanity_cuda
          retention-days: 1
